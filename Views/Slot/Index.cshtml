@model IEnumerable<Asr.Models.Slot>

@{
    ViewData["Title"] = "Index";
}

<h1>List of Slot</h1>

<p>
    <a asp-action="Create">Create New Slot</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Room)
            </th>
            <th> @Html.DisplayNameFor(model => model.StartTime.Date)</th>
            <th>
                @Html.DisplayNameFor(model => model.Staff)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Room.RoomID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartTime.Date)
            </td>   
            <td>
                @Html.DisplayFor(modelItem => item.Staff.StaffID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.StudentID)
            </td>
            <td>
                @*
        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
                <a asp-action="Edit" asp-route-id="@item.Room.RoomID" asp-route-dateslot="@item.StartTime">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Room.RoomID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Room.RoomID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
